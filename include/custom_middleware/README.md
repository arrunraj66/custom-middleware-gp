# Public API

Only headers intended for middleware users belong in this directory.

Future applications and the GUI team will include files from this location. Public headers must not expose private transport, database, Qt, ROS or SICK implementation types.

The first public type will be added only after the build-system lesson is complete.
