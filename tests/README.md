# Tests

Tests are added with every middleware feature.

The first lessons may use small executable tests so that every operation is visible. GoogleTest will be introduced after the basic build and linking process is understood.

A feature is not considered complete until its expected behavior and at least one failure or boundary case are tested.
