# Private implementation

Middleware implementation files belong here.

Planned subdirectories will be introduced only when their first component is written:

- `core/`
- `serialization/`
- `transport/`
- `storage/`

Applications must use the public API instead of including private files from this directory.
